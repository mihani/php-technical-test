{% extends 'base.html.twig' %}

{% block title %}Edit Trip{% endblock %}

{% block body %}
<div class="row">
    <div class="col-lg-7">
        <h1 class="mt-4">Edit Trip</h1>
        <div class="card mb-4">
            <div class="card-body">
                {{ include('trip/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
            <div class="card-footer">
                <a class="btn btn-done" href="{{ path('trip_index') }}"><i class="fas fa-chevron-left"></i> Back to list</a>
            </div>
        </div>
    </div>
</div>
{{ include('trip/_delete_form.html.twig') }}
{% endblock %}
